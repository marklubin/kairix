{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/home/kairix"],
      "description": "Access to filesystem operations"
    }
  }
}
