[project]
name = "perceptor-inspector"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
]

[tool.uv]
dev-dependencies = []

[tool.hatch.metadata]
allow-direct-references = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"